services:
  sprocketfactorydb:
    image: postgres:latest
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: sprocketFactory
    expose:
      - "5433"
    ports:
      - "5433:5432"
